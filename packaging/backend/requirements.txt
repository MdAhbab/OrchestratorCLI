# AI CLI Orchestrator - Backend Dependencies for PyInstaller Build
# Synced from ../../backend/requirements.txt plus packaging tools

# FastAPI and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Pydantic for data validation
pydantic==2.10.5
pydantic-settings==2.7.0
email-validator==2.1.0

# Database
aiosqlite==0.19.0

# File handling
python-multipart==0.0.6
aiofiles==23.2.1

# Security and encryption
cryptography==42.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client (for provider integrations).
httpx>=0.27,<0.28

# Utilities
python-dotenv==1.0.0

# Windows PTY (for real terminals in Parallel Terminals page)
pywinpty>=2.0.13 ; sys_platform == "win32"

# WebSocket support for FastAPI
websockets>=12.0

# Tray + browser launcher (installer bundle)
pystray>=0.19.5
pillow>=10.0.0

# YAML support (workspace templates)
pyyaml==6.0.1

# Packaging
pyinstaller>=6.8.0
